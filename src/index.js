import MC from './MC';
import MCWidget from './MCWidget';
import { onTouchStart, onTouchMove, onTouchEnd } from "./movements";

export {
    MC,
    MCWidget,
    onTouchStart,
    onTouchMove,
    onTouchEnd
}
